[tox]
envlist=lint
skipsdist=True

[testenv:lint]
deps=
    ansible-lint
    yamllint
whitelist_externals=
    find
commands=
    yamllint -s .
    find . -name "*.yml" -not -path ".tox" -exec ansible-lint \{\} ;
